"""Package marker so ``importlib.resources`` can address ``empty.xlsx`` by package name.

See ``client.excel_writer`` (workbook create-when-missing) and
``tests/setup/generate_empty_workbook_fixture.py`` (regenerates ``empty.xlsx``).
"""
